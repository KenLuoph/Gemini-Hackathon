name: gemini_life_planner
description: 基于 Gemini AI 的智能生活规划助手
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI 组件
  cupertino_icons: ^1.0.2
  
  # 网络请求
  http: ^1.1.0
  dio: ^5.4.0
  
  # 状态管理
  provider: ^6.1.1
  
  # JSON 序列化
  json_annotation: ^4.8.1
  
  # 本地存储
  shared_preferences: ^2.2.2
  
  # 日期时间处理
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^3.0.1
  build_runner: ^2.4.7
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  
  # assets:
  #   - images/
  
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf

